---
const modules = [
  { title: 'Dashboard', icon: '📊', slug: 'dashboard', featured: true, enabled: true },
  { title: 'My Card', icon: '📧', slug: 'my-card', featured: true, enabled: true },
  { title: 'People Hub', icon: '👥', slug: 'people-hub', featured: true, enabled: true },
  { title: 'Leave', icon: '🌴', slug: 'leave-management', featured: true, enabled: true },
  { title: 'Attendance', icon: '⏰', slug: 'attendance', featured: true, enabled: true },
  { title: 'Performance', icon: '📈', slug: 'performance', enabled: false },
  { title: 'Discipline', icon: '⚖️', slug: 'discipline', enabled: false },
  { title: 'Training', icon: '🎓', slug: 'training', enabled: false },
  { title: 'Onboarding', icon: '📝', slug: 'onboarding', enabled: false },
  { title: 'Asset Management', icon: '💼', slug: 'asset-management', enabled: false },
  { title: 'Announcement', icon: '📢', slug: 'announcement', enabled: false },
  { title: 'Project Management', icon: '🗂️', slug: 'project-management', enabled: false },
  { title: 'Payroll', icon: '💰', slug: 'payroll', enabled: false },
  { title: 'User Management', icon: '🧑‍💼', slug: 'user-management', enabled: false },
  { title: 'Qualification', icon: '🎖️', slug: 'qualification', enabled: false },
  { title: 'Configuration', icon: '⚙️', slug: 'configuration', enabled: false },
  { title: 'Audit Trail', icon: '🔍', slug: 'audit-trail', enabled: false },
];

const faqs = [
  {
    question: "Is Screen HRM free to use?",
    answer: "Yes, Screen HRM is completely free for teams with up to 5 employees. There are no hidden costs or feature limitations in the free tier."
  },
  {
    question: "How secure is my employee data?",
    answer: "We use bank-level encryption and security protocols to keep your data safe. Our system complies with GDPR, CCPA, and other global privacy regulations."
  },
  {
    question: "Can I import data from my current HR system?",
    answer: "Absolutely! We provide easy migration tools and personalized support to help you smoothly transition from your existing system."
  },
  {
    question: "How long does implementation take?",
    answer: "Most customers are up and running within hours. Basic setup takes just minutes, and our step-by-step guides help you configure more advanced features at your own pace."
  },
  {
    question: "What kind of support do you offer?",
    answer: "We provide 24/7 email support for all users, live chat during business hours, and priority phone support for premium plans. Our comprehensive help center also includes tutorials and guides."
  }
];
---
<section class="bg-brand-light py-16 relative overflow-hidden">
  <!-- Animated background patterns -->
  <div class="absolute inset-0 opacity-30">
    <div class="absolute top-0 left-0 w-72 h-72 bg-brand-primary/20 rounded-full filter blur-3xl animate-blob"></div>
    <div class="absolute top-0 right-0 w-72 h-72 bg-brand-accent/20 rounded-full filter blur-3xl animate-blob animation-delay-2000"></div>
    <div class="absolute bottom-0 left-1/2 w-72 h-72 bg-purple-400/20 rounded-full filter blur-3xl animate-blob animation-delay-4000"></div>
  </div>

  <div class="container mx-auto px-4 relative z-10">
    <div class="text-center mb-12" data-aos="fade-up">
      <h2 class="text-4xl md:text-5xl font-bold mb-4 text-brand-dark">
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-brand-accent animate-gradient">Modular</span> Solutions
      </h2>
      <p class="text-gray-600 max-w-2xl mx-auto text-lg">
        Pick exactly what you need and leave behind anything you don't. Our modules are designed to solve your biggest HR problems—start with what you need now and add as you grow.
      </p>
    </div>

    {/* Featured Modules - First Row */}
    <div class="mb-10">
      <h3 class="font-semibold text-xl mb-6 text-brand-primary animate-slide-in" data-aos="fade-right">Core Modules</h3>
      <div class="grid gap-6 grid-cols-1 sm:grid-cols-2 lg:grid-cols-5">
        {modules.filter(m => m.featured).map((module, index) => (
          module.enabled ? (
            <a 
              href={`/modules/${module.slug}`} 
              class="group relative bg-white p-6 sm:p-8 rounded-xl shadow-lg hover:shadow-2xl transition-all duration-500 flex flex-col items-center text-center hover:ring-2 hover:ring-brand-primary focus:outline-none border border-brand-primary/20 transform hover:-translate-y-2"
              data-aos="zoom-in"
              data-aos-delay={index * 100}
            >
              <div class="absolute inset-0 bg-gradient-to-br from-brand-primary/0 to-brand-accent/0 group-hover:from-brand-primary/10 group-hover:to-brand-accent/10 rounded-xl transition-all duration-500"></div>
              <div class="relative text-5xl mb-4 transform transition-all duration-500 group-hover:scale-125 group-hover:rotate-12">{module.icon}</div>
              <h3 class="relative text-xl font-bold mb-3 text-brand-dark group-hover:text-brand-primary transition-colors">{module.title}</h3>
              <div class="relative bg-brand-primary/10 text-brand-primary text-xs font-medium px-3 py-1 rounded-full group-hover:bg-brand-primary group-hover:text-white transition-all">Featured</div>
            </a>
          ) : (
            <div
              tabindex="0"
              class="group relative bg-white p-6 sm:p-8 rounded-xl shadow-lg hover:shadow-2xl transition-all duration-500 flex flex-col items-center text-center hover:ring-2 hover:ring-brand-primary focus:outline-none border border-brand-primary/20 cursor-pointer transform hover:-translate-y-2"
              onClick={(e) => e.preventDefault()}
              onKeyDown={(e) => e.preventDefault()}
              data-aos="zoom-in"
              data-aos-delay={index * 100}
            >
              <div class="absolute inset-0 bg-gradient-to-br from-brand-primary/0 to-brand-accent/0 group-hover:from-brand-primary/10 group-hover:to-brand-accent/10 rounded-xl transition-all duration-500"></div>
              <div class="relative text-5xl mb-4 transform transition-all duration-500 group-hover:scale-125 group-hover:rotate-12">{module.icon}</div>
              <h3 class="relative text-xl font-bold mb-3 text-brand-dark group-hover:text-brand-primary transition-colors">{module.title}</h3>
              <div class="relative bg-brand-primary/10 text-brand-primary text-xs font-medium px-3 py-1 rounded-full group-hover:bg-brand-primary group-hover:text-white transition-all">Featured</div>
            </div>
          )
        ))}
      </div>
    </div>
    
    {/* Additional Modules */}
    <div>
      <h3 class="font-semibold text-xl mb-6 text-gray-600" data-aos="fade-right">Additional Modules</h3>
      <div class="grid gap-4 grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6">
        {modules.filter(m => !m.featured).map((module, index) => (
          module.enabled ? (
            <a 
              href={`/modules/${module.slug}`} 
              class="group bg-white p-4 rounded-lg shadow hover:shadow-lg transition-all duration-300 flex flex-col items-center text-center hover:bg-gradient-to-br hover:from-brand-primary/5 hover:to-brand-accent/5 transform hover:scale-105"
              data-aos="fade-up"
              data-aos-delay={index * 50}
            >
              <div class="text-3xl mb-2 transform transition-transform duration-300 group-hover:scale-110">{module.icon}</div>
              <h3 class="text-sm font-medium text-gray-800 group-hover:text-brand-primary transition-colors">{module.title}</h3>
            </a>
          ) : (
            <div
              tabindex="0"
              class="group bg-white p-4 rounded-lg shadow hover:shadow-lg transition-all duration-300 flex flex-col items-center text-center hover:bg-gradient-to-br hover:from-brand-primary/5 hover:to-brand-accent/5 cursor-pointer transform hover:scale-105"
              onClick={(e) => e.preventDefault()}
              onKeyDown={(e) => e.preventDefault()}
              data-aos="fade-up"
              data-aos-delay={index * 50}
            >
              <div class="text-3xl mb-2 transform transition-transform duration-300 group-hover:scale-110">{module.icon}</div>
              <h3 class="text-sm font-medium text-gray-800 group-hover:text-brand-primary transition-colors">{module.title}</h3>
            </div>
          )
        ))}
      </div>
    </div>
    
    {/* FAQ Section */}
    <div class="mt-24 max-w-4xl mx-auto" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold text-center mb-12 text-brand-dark">
        Frequently Asked <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-brand-accent">Questions</span>
      </h2>
      
      <div class="space-y-6">
        {faqs.map((faq, index) => (
          <div 
            class="bg-white rounded-xl shadow-md overflow-hidden border border-gray-100 hover:shadow-lg transition-all duration-300"
            data-aos="fade-up"
            data-aos-delay={index * 100}
          >
            <details class="group">
              <summary class="flex justify-between items-center p-6 cursor-pointer list-none text-lg font-medium text-brand-dark hover:bg-brand-light/30 transition-colors">
                <span>{faq.question}</span>
                <span class="transition-all duration-300 group-open:rotate-180 group-open:text-brand-primary">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-5 w-5">
                    <polyline points="6 9 12 15 18 9"></polyline>
                  </svg>
                </span>
              </summary>
              <div class="px-6 pb-6 pt-0 animate-accordion">
                <p class="text-gray-600">{faq.answer}</p>
              </div>
            </details>
          </div>
        ))}
      </div>
      
      <div class="mt-10 text-center" data-aos="zoom-in">
        <p class="text-gray-600 mb-6">Still have questions? We're here to help.</p>
        <a href="/contact-us" class="group inline-block bg-brand-primary text-white font-medium px-6 py-3 rounded-lg hover:bg-brand-primary/90 transition-all duration-300 transform hover:scale-105 hover:shadow-lg">
          <span class="inline-flex items-center">
            Contact Support
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 transform transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </span>
        </a>
      </div>
    </div>
  </div>

  <style>
    @keyframes blob {
      0%, 100% {
        transform: translate(0px, 0px) scale(1);
      }
      33% {
        transform: translate(30px, -50px) scale(1.1);
      }
      66% {
        transform: translate(-20px, 20px) scale(0.9);
      }
    }
    
    @keyframes gradient {
      0%, 100% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
    }
    
    @keyframes slide-in {
      from {
        opacity: 0;
        transform: translateX(-30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
    
    @keyframes accordion {
      from {
        opacity: 0;
        max-height: 0;
      }
      to {
        opacity: 1;
        max-height: 500px;
      }
    }
    
    .animate-blob {
      animation: blob 7s infinite;
    }
    
    .animate-gradient {
      background-size: 200% 200%;
      animation: gradient 4s ease infinite;
    }
    
    .animate-slide-in {
      animation: slide-in 0.6s ease-out;
    }
    
    .animate-accordion {
      animation: accordion 0.3s ease-out;
    }
    
    .animation-delay-2000 {
      animation-delay: 2s;
    }
    
    .animation-delay-4000 {
      animation-delay: 4s;
    }
  </style>
</section>